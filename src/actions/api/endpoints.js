const API_PATH = 'api/v1';

const endpointMapping = {
  META_DYNAMICS: `${API_PATH}/meta/dynamics`,
  META_HISTORY: `${API_PATH}/meta/history`,
  META_CONFIG: `${API_PATH}/meta/config`,

  SUGGESTIONS: `${API_PATH}/suggestions`,
};

export default endpointMapping;
